// Generated by CoffeeScript 1.6.2
(function() {
  var chat, model, userController, userForm;

  if (typeof tddjs === "undefined" || typeof document === "undefined" || !document.getElementById || !Object.create || !tddjs.namespace("chat").userFormController) {
    alert("Browser is not supported");
    return;
  }
  chat = tddjs.chat;
  model = {};
  userForm = document.getElementById("userForm");
  userController = Object.create(chat.userFormController);
  userController.setModel(model);
  userController.setView(userForm);
  return userController.observe("user", function(user) {
    return alert("Welcome, " + user);
  });
})();
